let exampleManager = (() => {

    // gloabl variables declarations

    function initialize() {
        
    }

    initialize();

    return Object.freeze({
        
    });
})();

export { exampleManager };