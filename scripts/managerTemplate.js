let exampleManager = (() => {

    const gl = {};

    function initialize() {
        
    }

    initialize();

    return {
        
    }
})();

Object.freeze(exampleManager);

export { exampleManager };